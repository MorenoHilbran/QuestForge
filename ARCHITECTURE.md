# 📱 QuestForge - Screen Flow & Architecture

## Application Flow Diagram

```
┌─────────────────────────────────────────────────────────────┐
│                      APP LAUNCH                              │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
            ┌────────────────┐
            │  Login Screen  │
            └────────┬───────┘
                     │
                     ├──► Admin Login ──────┐
                     │                      │
                     └──► User Login        │
                            │               │
                            ▼               ▼
                    ┌──────────────────────────┐
                    │     Home Screen          │
                    └──────────┬───────────────┘
                               │
                ┏━━━━━━━━━━━━━━┻━━━━━━━━━━━━━━┓
                ▼                              ▼
    ┌─────────────────────┐      ┌─────────────────────┐
    │   Admin Features    │      │   User Features     │
    └──────────┬──────────┘      └──────────┬──────────┘
               │                             │
               ▼                             ▼
    ┌─────────────────────┐      ┌─────────────────────┐
    │  Admin Projects     │      │  User Projects      │
    │     Screen          │      │     Screen          │
    └──────────┬──────────┘      └──────────┬──────────┘
               │                             │
               ├─► Add Project               ├─► Join Project
               │    Screen                   │    Screen
               │                             │
               └──────────┬──────────────────┘
                          │
                          ▼
                ┌──────────────────┐
                │  Project Detail  │
                │     Screen       │
                └────────┬─────────┘
                         │
            ┏━━━━━━━━━━━━┻━━━━━━━━━━━━┓
            ▼                          ▼
    ┌──────────────┐         ┌──────────────┐
    │    Tasks     │         │  Milestones  │
    │   Screen     │         │   Screen     │
    └──────────────┘         └──────────────┘
```

## Screen Details

### 🔐 Authentication Flow

```
Login Screen
├── Input: Name, Email
├── Toggle: Admin/User
└── Action: Login → Navigate to Home
```

### 🏠 Home Screen

```
Home Screen
├── User Info Card (Name, Email, Role Badge)
├── Admin: "Manage Projects" Button → Admin Projects Screen
└── User: "My Projects" Button → User Projects Screen
```

### 👔 Admin Flow

```
Admin Projects Screen
├── "Create New Project" Button → Add Project Screen
└── Project Cards List
    ├── Project Name
    ├── Project Code
    ├── Stats (Members, Tasks)
    ├── Deadline
    └── Tap → Project Detail Screen

Add Project Screen
├── Input: Project Name
├── Input: Description
├── Date Picker: Deadline
└── Action: Create → Auto-generate code & save
```

### 👤 User Flow

```
User Projects Screen
├── "Join Project" Button → Join Project Screen
└── Project Cards List
    ├── Project Name
    ├── Role Badge
    ├── Progress Stats
    └── Tap → Project Detail Screen

Join Project Screen
├── Input: Project Code
├── Toggle: Solo/Multiplayer Mode
├── Role Selection (if Multiplayer)
│   ├── Frontend
│   ├── Backend
│   ├── Project Manager
│   └── UI/UX
└── Action: Join → Add to project
```

### 📊 Project Detail Screen

```
Project Detail Screen
├── Project Code Card (Large Display)
├── Progress Card
│   ├── Progress Bar
│   └── Completion Stats
├── Stats Cards
│   ├── Members Count
│   └── Milestones Count
├── Deadline Card
├── Your Role Card (if User)
├── "View Tasks" Button → Tasks Screen
└── "View Milestones" Button → Milestones Screen
```

### ✅ Tasks Screen

```
Tasks Screen
├── Role Filter Bar (PM/Admin only)
├── "Add Task" Button (PM/Admin only) → Add Task Dialog
├── Task Cards List
│   ├── Task Title
│   ├── Priority Badge
│   ├── Role Badge
│   ├── Status (Color-coded)
│   └── Tap → Task Detail Dialog
│
└── Task Detail Dialog
    ├── Title
    ├── Description
    ├── Role
    ├── Priority
    ├── Current Status
    └── Status Update Buttons
```

### 🎯 Milestones Screen

```
Milestones Screen
├── "Add Milestone" Button (PM/Admin only) → Add Milestone Dialog
├── Milestone Cards List
│   ├── Milestone Name
│   ├── Progress Bar (Auto-calculated)
│   ├── Status
│   └── Target Date
│
└── Add Milestone Dialog
    ├── Input: Name
    ├── Date Picker: Target Date
    └── Action: Create
```

## Data Flow Architecture

```
┌─────────────────────────────────────────────────────────┐
│                    UI LAYER (Screens)                    │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│              STATE MANAGEMENT (Providers)                │
│  ┌─────────────────┐      ┌──────────────────┐         │
│  │  AuthProvider   │      │ ProjectProvider  │         │
│  │  - Login        │      │ - Projects       │         │
│  │  - Logout       │      │ - Tasks          │         │
│  │  - User State   │      │ - Milestones     │         │
│  └─────────────────┘      │ - Activity Logs  │         │
│                            └──────────────────┘         │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│                   DATA LAYER (Models)                    │
│  ┌─────────────┬─────────────┬──────────────┐          │
│  │ UserModel   │ProjectModel │ TaskModel    │          │
│  ├─────────────┼─────────────┼──────────────┤          │
│  │ProjectUser  │ Milestone   │ActivityLog   │          │
│  └─────────────┴─────────────┴──────────────┘          │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│              PERSISTENCE LAYER                           │
│  ┌──────────────────┐    ┌──────────────────┐          │
│  │SharedPreferences │    │  In-Memory Store │          │
│  │ (User Session)   │    │  (App Data)      │          │
│  └──────────────────┘    └──────────────────┘          │
└─────────────────────────────────────────────────────────┘
```

## Widget Tree Structure

```
MaterialApp
└── MultiProvider
    ├── AuthProvider
    └── ProjectProvider
        └── Consumer<AuthProvider>
            ├── LoginScreen (if not logged in)
            └── HomeScreen (if logged in)
                ├── Admin Path
                │   └── AdminProjectsScreen
                │       ├── AddProjectScreen
                │       └── ProjectDetailScreen
                └── User Path
                    └── UserProjectsScreen
                        ├── JoinProjectScreen
                        └── ProjectDetailScreen
                            ├── TasksScreen
                            └── MilestonesScreen
```

## Custom Widgets Architecture

```
┌─────────────────────────────────────────┐
│        Reusable Neo Widgets             │
├─────────────────────────────────────────┤
│  NeoButton                              │
│  ├── Container (Shadow)                 │
│  ├── Container (Main)                   │
│  └── Material + InkWell                 │
├─────────────────────────────────────────┤
│  NeoCard                                │
│  ├── Container (Shadow)                 │
│  ├── Container (Main + Border)          │
│  └── Content (with optional tap)        │
├─────────────────────────────────────────┤
│  NeoTextField                           │
│  ├── Label Text                         │
│  ├── Container (Shadow)                 │
│  └── TextFormField (with borders)       │
├─────────────────────────────────────────┤
│  NeoProgressBar                         │
│  ├── Container (Shadow)                 │
│  ├── Container (Background)             │
│  └── Progress Indicator (with %)        │
└─────────────────────────────────────────┘
```

## Role-Based Access Control

```
┌──────────────────────────────────────────────────────┐
│                  ADMIN CAPABILITIES                   │
├──────────────────────────────────────────────────────┤
│  ✓ Create Projects                                   │
│  ✓ View All Admin Projects                          │
│  ✓ Add Tasks (All Roles)                            │
│  ✓ Add/Edit/Delete Milestones                       │
│  ✓ View All Project Members                         │
│  ✓ Access Full Project Dashboard                    │
└──────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────┐
│            PROJECT MANAGER CAPABILITIES               │
├──────────────────────────────────────────────────────┤
│  ✓ Add Tasks (All Roles)                            │
│  ✓ Add/Edit/Delete Milestones                       │
│  ✓ View All Tasks                                    │
│  ✓ View All Project Members                         │
│  ✓ Access Full Project Dashboard                    │
└──────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────┐
│               USER CAPABILITIES                       │
├──────────────────────────────────────────────────────┤
│  ✓ Join Projects (Solo or by Role)                  │
│  ✓ View Projects They're Part Of                    │
│  ✓ View Role-Specific Tasks                         │
│  ✓ Update Task Status                               │
│  ✓ View Milestones (Read-Only)                      │
│  ✓ View Project Progress                            │
└──────────────────────────────────────────────────────┘
```

## Color Coding System

```
┌─────────────────────────────────────────┐
│         ROLES → COLORS                  │
├─────────────────────────────────────────┤
│  Frontend      → Purple  (#6B5CE7)      │
│  Backend       → Green   (#00D4AA)      │
│  Project Mgr   → Red     (#FF6B6B)      │
│  UI/UX         → Orange  (#FF8C42)      │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│      TASK STATUS → COLORS               │
├─────────────────────────────────────────┤
│  To-do         → Gray    (#E5E5E5)      │
│  In Progress   → Yellow  (#FFDA26)      │
│  Review        → Cyan    (#00E0E0)      │
│  Done          → Green   (#51CF66)      │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│      PRIORITY → COLORS                  │
├─────────────────────────────────────────┤
│  High          → Red     (#FF6B6B)      │
│  Medium        → Yellow  (#FFDA26)      │
│  Low           → Green   (#51CF66)      │
└─────────────────────────────────────────┘
```

---

**This architecture provides a clear separation of concerns, making the app maintainable and scalable!**
