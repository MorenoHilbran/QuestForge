-- Check user_projects RLS policies untuk DELETE operation
SELECT 
    schemaname,
    tablename,
    policyname,
    permissive,
    roles,
    cmd,
    qual,
    with_check
FROM pg_policies
WHERE tablename = 'user_projects'
  AND cmd = 'DELETE';
